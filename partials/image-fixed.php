<picture class="{{classes}}" style="--aspect-ratio:{{aspect_ratio}}">
    <img
        src="{{src}}"
        width="{{width}}"
        height="{{height}}"
        alt="{{alt}}"
        loading="{{loading}}"
        decoding="{{decoding}}"
        srcset="{{srcset}}"
    />
</picture>
