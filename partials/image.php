<picture class="{{classes}}" style="--aspect-ratio:{{aspect_ratio}}">
<img src="{{src}}"
     srcset="{{srcset}}"
     sizes="{{sizes}}"
     width="{{width}}" height="{{height}}" alt="{{alt}}" loading="{{loading}}" decoding="{{decoding}}"
/>
</picture>
